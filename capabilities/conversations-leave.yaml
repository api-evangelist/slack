naftiko: 1.0.0-alpha2
info:
  label: Slack Conversations API — Leave
  description: 'Slack Conversations API — Leave. 1 operations. Lead operation: Slack Post Conversations Leave. Self-contained
    Naftiko capability covering one Slack business surface.'
  tags:
  - Slack
  - Leave
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: conversations-leave
    baseUri: ''
    description: Slack Conversations API — Leave business capability. Self-contained, no shared references.
    resources:
    - name: conversations.leave
      path: /conversations.leave
      operations:
      - name: postconversationsleave
        method: POST
        description: Slack Post Conversations Leave
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `conversations:write`'
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: conversations-leave-rest
    port: 8080
    description: REST adapter for Slack Conversations API — Leave. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/conversations-leave
      name: conversations-leave
      description: REST surface for conversations.leave.
      operations:
      - method: POST
        name: postconversationsleave
        description: Slack Post Conversations Leave
        call: conversations-leave.postconversationsleave
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: conversations-leave-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Conversations API — Leave. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: slack-post-conversations-leave
      description: Slack Post Conversations Leave
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: conversations-leave.postconversationsleave
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
