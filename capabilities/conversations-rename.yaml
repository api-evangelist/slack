naftiko: 1.0.0-alpha2
info:
  label: Slack Conversations API — Rename
  description: 'Slack Conversations API — Rename. 1 operations. Lead operation: Slack Post Conversations Rename. Self-contained
    Naftiko capability covering one Slack business surface.'
  tags:
  - Slack
  - Rename
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: conversations-rename
    baseUri: ''
    description: Slack Conversations API — Rename business capability. Self-contained, no shared references.
    resources:
    - name: conversations.rename
      path: /conversations.rename
      operations:
      - name: postconversationsrename
        method: POST
        description: Slack Post Conversations Rename
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `conversations:write`'
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: conversations-rename-rest
    port: 8080
    description: REST adapter for Slack Conversations API — Rename. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/conversations-rename
      name: conversations-rename
      description: REST surface for conversations.rename.
      operations:
      - method: POST
        name: postconversationsrename
        description: Slack Post Conversations Rename
        call: conversations-rename.postconversationsrename
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: conversations-rename-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Conversations API — Rename. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: slack-post-conversations-rename
      description: Slack Post Conversations Rename
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: conversations-rename.postconversationsrename
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
