naftiko: 1.0.0-alpha2
info:
  label: Slack Admin API — Add
  description: 'Slack Admin API — Add. 1 operations. Lead operation: Slack Post Admin Emoji Add. Self-contained Naftiko capability
    covering one Slack business surface.'
  tags:
  - Slack
  - Add
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-add
    baseUri: ''
    description: Slack Admin API — Add business capability. Self-contained, no shared references.
    resources:
    - name: admin.emoji.add
      path: /admin.emoji.add
      operations:
      - name: postadminemojiadd
        method: POST
        description: Slack Post Admin Emoji Add
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: admin-add-rest
    port: 8080
    description: REST adapter for Slack Admin API — Add. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/admin-emoji-add
      name: admin-emoji-add
      description: REST surface for admin.emoji.add.
      operations:
      - method: POST
        name: postadminemojiadd
        description: Slack Post Admin Emoji Add
        call: admin-add.postadminemojiadd
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-add-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Admin API — Add. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: slack-post-admin-emoji-add
      description: Slack Post Admin Emoji Add
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: admin-add.postadminemojiadd
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
