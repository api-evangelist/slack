naftiko: 1.0.0-alpha2
info:
  label: Slack Calls API — Post
  description: 'Slack Calls API — Post. 5 operations. Lead operation: Slack Post Calls Add. Self-contained Naftiko capability
    covering one Slack business surface.'
  tags:
  - Slack
  - Post
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: calls-post
    baseUri: ''
    description: Slack Calls API — Post business capability. Self-contained, no shared references.
    resources:
    - name: calls.add
      path: /calls.add
      operations:
      - name: callsadd
        method: POST
        description: Slack Post Calls Add
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `calls:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: calls.end
      path: /calls.end
      operations:
      - name: callsend
        method: POST
        description: Slack Post Calls End
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `calls:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: calls.participants.add
      path: /calls.participants.add
      operations:
      - name: callsparticipantsadd
        method: POST
        description: Slack Post Calls Participants Add
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `calls:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: calls.participants.remove
      path: /calls.participants.remove
      operations:
      - name: callsparticipantsremove
        method: POST
        description: Slack Post Calls Participants Remove
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `calls:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: calls.update
      path: /calls.update
      operations:
      - name: callsupdate
        method: POST
        description: Slack Post Calls Update
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `calls:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: calls-post-rest
    port: 8080
    description: REST adapter for Slack Calls API — Post. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/calls-add
      name: calls-add
      description: REST surface for calls.add.
      operations:
      - method: POST
        name: callsadd
        description: Slack Post Calls Add
        call: calls-post.callsadd
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/calls-end
      name: calls-end
      description: REST surface for calls.end.
      operations:
      - method: POST
        name: callsend
        description: Slack Post Calls End
        call: calls-post.callsend
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/calls-participants-add
      name: calls-participants-add
      description: REST surface for calls.participants.add.
      operations:
      - method: POST
        name: callsparticipantsadd
        description: Slack Post Calls Participants Add
        call: calls-post.callsparticipantsadd
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/calls-participants-remove
      name: calls-participants-remove
      description: REST surface for calls.participants.remove.
      operations:
      - method: POST
        name: callsparticipantsremove
        description: Slack Post Calls Participants Remove
        call: calls-post.callsparticipantsremove
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/calls-update
      name: calls-update
      description: REST surface for calls.update.
      operations:
      - method: POST
        name: callsupdate
        description: Slack Post Calls Update
        call: calls-post.callsupdate
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: calls-post-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Calls API — Post. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: slack-post-calls-add
      description: Slack Post Calls Add
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: calls-post.callsadd
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-calls-end
      description: Slack Post Calls End
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: calls-post.callsend
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-calls-participants-add
      description: Slack Post Calls Participants Add
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: calls-post.callsparticipantsadd
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-calls-participants-remove
      description: Slack Post Calls Participants Remove
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: calls-post.callsparticipantsremove
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-calls-update
      description: Slack Post Calls Update
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: calls-post.callsupdate
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
