naftiko: 1.0.0-alpha2
info:
  label: Slack Admin API — Deny
  description: 'Slack Admin API — Deny. 1 operations. Lead operation: Slack Post Admin Inviterequests Deny. Self-contained
    Naftiko capability covering one Slack business surface.'
  tags:
  - Slack
  - Deny
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-deny
    baseUri: ''
    description: Slack Admin API — Deny business capability. Self-contained, no shared references.
    resources:
    - name: admin.inviteRequests.deny
      path: /admin.inviteRequests.deny
      operations:
      - name: postadmininviterequestsdeny
        method: POST
        description: Slack Post Admin Inviterequests Deny
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `admin.invites:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: admin-deny-rest
    port: 8080
    description: REST adapter for Slack Admin API — Deny. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/admin-inviterequests-deny
      name: admin-inviterequests-deny
      description: REST surface for admin.inviteRequests.deny.
      operations:
      - method: POST
        name: postadmininviterequestsdeny
        description: Slack Post Admin Inviterequests Deny
        call: admin-deny.postadmininviterequestsdeny
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-deny-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Admin API — Deny. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: slack-post-admin-inviterequests-deny
      description: Slack Post Admin Inviterequests Deny
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: admin-deny.postadmininviterequestsdeny
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
