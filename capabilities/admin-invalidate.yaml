naftiko: 1.0.0-alpha2
info:
  label: Slack Admin API — Invalidate
  description: 'Slack Admin API — Invalidate. 1 operations. Lead operation: Slack Post Admin Users Session Invalidate. Self-contained
    Naftiko capability covering one Slack business surface.'
  tags:
  - Slack
  - Invalidate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-invalidate
    baseUri: ''
    description: Slack Admin API — Invalidate business capability. Self-contained, no shared references.
    resources:
    - name: admin.users.session.invalidate
      path: /admin.users.session.invalidate
      operations:
      - name: postadminuserssessioninvalidate
        method: POST
        description: Slack Post Admin Users Session Invalidate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `admin.users:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: admin-invalidate-rest
    port: 8080
    description: REST adapter for Slack Admin API — Invalidate. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/admin-users-session-invalidate
      name: admin-users-session-invalidate
      description: REST surface for admin.users.session.invalidate.
      operations:
      - method: POST
        name: postadminuserssessioninvalidate
        description: Slack Post Admin Users Session Invalidate
        call: admin-invalidate.postadminuserssessioninvalidate
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-invalidate-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Admin API — Invalidate. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: slack-post-admin-users-session
      description: Slack Post Admin Users Session Invalidate
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: admin-invalidate.postadminuserssessioninvalidate
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
