naftiko: 1.0.0-alpha2
info:
  label: Slack Conversations API — Open
  description: 'Slack Conversations API — Open. 1 operations. Lead operation: Slack Post Conversations Open. Self-contained
    Naftiko capability covering one Slack business surface.'
  tags:
  - Slack
  - Open
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: conversations-open
    baseUri: ''
    description: Slack Conversations API — Open business capability. Self-contained, no shared references.
    resources:
    - name: conversations.open
      path: /conversations.open
      operations:
      - name: postconversationsopen
        method: POST
        description: Slack Post Conversations Open
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `conversations:write`'
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: conversations-open-rest
    port: 8080
    description: REST adapter for Slack Conversations API — Open. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/conversations-open
      name: conversations-open
      description: REST surface for conversations.open.
      operations:
      - method: POST
        name: postconversationsopen
        description: Slack Post Conversations Open
        call: conversations-open.postconversationsopen
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: conversations-open-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Conversations API — Open. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: slack-post-conversations-open
      description: Slack Post Conversations Open
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: conversations-open.postconversationsopen
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
