naftiko: 1.0.0-alpha2
info:
  label: Slack Views API — Open
  description: 'Slack Views API — Open. 1 operations. Lead operation: Slack Get Views Open. Self-contained Naftiko capability
    covering one Slack business surface.'
  tags:
  - Slack
  - Open
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: views-open
    baseUri: ''
    description: Slack Views API — Open business capability. Self-contained, no shared references.
    resources:
    - name: views.open
      path: /views.open
      operations:
      - name: getviewsopen
        method: GET
        description: Slack Get Views Open
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `none`'
          required: true
        - name: trigger_id
          in: query
          type: string
          description: Exchange a trigger to post to the user.
          required: true
        - name: view
          in: query
          type: string
          description: A [view payload](/reference/surfaces/views). This must be a JSON-encoded string.
          required: true
  exposes:
  - type: rest
    namespace: views-open-rest
    port: 8080
    description: REST adapter for Slack Views API — Open. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/views-open
      name: views-open
      description: REST surface for views.open.
      operations:
      - method: GET
        name: getviewsopen
        description: Slack Get Views Open
        call: views-open.getviewsopen
        with:
          token: rest.token
          trigger_id: rest.trigger_id
          view: rest.view
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: views-open-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Views API — Open. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: slack-get-views-open
      description: Slack Get Views Open
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: views-open.getviewsopen
      with:
        token: tools.token
        trigger_id: tools.trigger_id
        view: tools.view
      outputParameters:
      - type: object
        mapping: $.
