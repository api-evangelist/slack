naftiko: 1.0.0-alpha2
info:
  label: Slack Users API — Post
  description: 'Slack Users API — Post. 5 operations. Lead operation: Slack Post Users Deletephoto. Self-contained Naftiko
    capability covering one Slack business surface.'
  tags:
  - Slack
  - Post
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-post
    baseUri: ''
    description: Slack Users API — Post business capability. Self-contained, no shared references.
    resources:
    - name: users.deletePhoto
      path: /users.deletePhoto
      operations:
      - name: postusersdeletephoto
        method: POST
        description: Slack Post Users Deletephoto
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: users.profile.set
      path: /users.profile.set
      operations:
      - name: postusersprofileset
        method: POST
        description: Slack Post Users Profile Set
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `users.profile:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: users.setActive
      path: /users.setActive
      operations:
      - name: postuserssetactive
        method: POST
        description: Slack Post Users Setactive
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `users:write`'
          required: true
    - name: users.setPhoto
      path: /users.setPhoto
      operations:
      - name: postuserssetphoto
        method: POST
        description: Slack Post Users Setphoto
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: users.setPresence
      path: /users.setPresence
      operations:
      - name: postuserssetpresence
        method: POST
        description: Slack Post Users Setpresence
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `users:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: users-post-rest
    port: 8080
    description: REST adapter for Slack Users API — Post. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users-deletephoto
      name: users-deletephoto
      description: REST surface for users.deletePhoto.
      operations:
      - method: POST
        name: postusersdeletephoto
        description: Slack Post Users Deletephoto
        call: users-post.postusersdeletephoto
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users-profile-set
      name: users-profile-set
      description: REST surface for users.profile.set.
      operations:
      - method: POST
        name: postusersprofileset
        description: Slack Post Users Profile Set
        call: users-post.postusersprofileset
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users-setactive
      name: users-setactive
      description: REST surface for users.setActive.
      operations:
      - method: POST
        name: postuserssetactive
        description: Slack Post Users Setactive
        call: users-post.postuserssetactive
        with:
          token: rest.token
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users-setphoto
      name: users-setphoto
      description: REST surface for users.setPhoto.
      operations:
      - method: POST
        name: postuserssetphoto
        description: Slack Post Users Setphoto
        call: users-post.postuserssetphoto
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users-setpresence
      name: users-setpresence
      description: REST surface for users.setPresence.
      operations:
      - method: POST
        name: postuserssetpresence
        description: Slack Post Users Setpresence
        call: users-post.postuserssetpresence
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-post-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Users API — Post. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: slack-post-users-deletephoto
      description: Slack Post Users Deletephoto
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: users-post.postusersdeletephoto
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-users-profile-set
      description: Slack Post Users Profile Set
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: users-post.postusersprofileset
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-users-setactive
      description: Slack Post Users Setactive
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: users-post.postuserssetactive
      with:
        token: tools.token
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-users-setphoto
      description: Slack Post Users Setphoto
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: users-post.postuserssetphoto
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: slack-post-users-setpresence
      description: Slack Post Users Setpresence
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: users-post.postuserssetpresence
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
