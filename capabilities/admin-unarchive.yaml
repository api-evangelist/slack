naftiko: 1.0.0-alpha2
info:
  label: Slack Admin API — Unarchive
  description: 'Slack Admin API — Unarchive. 1 operations. Lead operation: Slack Post Admin Conversations Unarchive. Self-contained
    Naftiko capability covering one Slack business surface.'
  tags:
  - Slack
  - Unarchive
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SLACK_API_KEY: SLACK_API_KEY
capability:
  consumes:
  - type: http
    namespace: admin-unarchive
    baseUri: ''
    description: Slack Admin API — Unarchive business capability. Self-contained, no shared references.
    resources:
    - name: admin.conversations.unarchive
      path: /admin.conversations.unarchive
      operations:
      - name: postadminconversationsunarchive
        method: POST
        description: Slack Post Admin Conversations Unarchive
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: token
          in: header
          type: string
          description: 'Authentication token. Requires scope: `admin.conversations:write`'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: admin-unarchive-rest
    port: 8080
    description: REST adapter for Slack Admin API — Unarchive. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/admin-conversations-unarchive
      name: admin-conversations-unarchive
      description: REST surface for admin.conversations.unarchive.
      operations:
      - method: POST
        name: postadminconversationsunarchive
        description: Slack Post Admin Conversations Unarchive
        call: admin-unarchive.postadminconversationsunarchive
        with:
          token: rest.token
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: admin-unarchive-mcp
    port: 9090
    transport: http
    description: MCP adapter for Slack Admin API — Unarchive. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: slack-post-admin-conversations-unarchive
      description: Slack Post Admin Conversations Unarchive
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: admin-unarchive.postadminconversationsunarchive
      with:
        token: tools.token
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
